package io.github.notfoundry.elevatorsimulator.floor;

/**
 * This represents the possible buttons that users of an elevator can push to go in a given direction.
 * Conversion from strings to these constants is expected to be performed through the Enum class valueOf method.
 * 
 * @author Mark
 */
public enum FloorButton {
	UP, DOWN
}
